{
  "name": "@phoenixaihub/agent-checkpoint-cli",
  "version": "1.0.0",
  "description": "CLI for agent checkpoint management",
  "main": "dist/index.js",
  "bin": {
    "checkpoint": "dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format cjs --target node18",
    "dev": "tsup src/cli.ts --format cjs --target node18 --watch",
    "test": "echo \"CLI tests not yet implemented\"",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src"
  },
  "dependencies": {
    "@phoenixaihub/agent-checkpoint-core": "workspace:*",
    "commander": "^11.0.0",
    "chalk": "^5.3.0",
    "ora": "^7.0.1",
    "table": "^6.8.1"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "vitest": "^1.6.0",
    "@types/node": "^20.14.0"
  },
  "keywords": [
    "checkpoint",
    "cli",
    "agent",
    "restore"
  ],
  "author": "Phoenix AI",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/phoenix-assistant/agent-checkpoint.git",
    "directory": "packages/cli"
  }
}